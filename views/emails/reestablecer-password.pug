<!DOCTYPE html>
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            style
                include ./email.css
            
    body
        h2 Reestablecer contraseña
        p Hola, has solicitado reestablecer tu contraseña, has click en el siguiente enlace para reestablecer tu contraseña, este enlace solo es temporal
        a(href=`${resetUrl}`) Ir al enelace para reestablcer la contraseña
        p Si no puedes acceder al enlace, visita #{resetUrl}
        p Si no solicitaste este e-mail, ignorelo por favor